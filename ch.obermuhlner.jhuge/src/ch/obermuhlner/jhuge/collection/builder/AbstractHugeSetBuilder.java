package ch.obermuhlner.jhuge.collection.builder;

import java.util.Set;

/**
 * Abstract base class to implement a {@link SetBuilder} for a huge {@link Set}.
 *
 * @param <E> the type of the element in the {@link Set}
 */
public abstract class AbstractHugeSetBuilder<E> extends AbstractHugeCollectionBuilder<E> implements SetBuilder<E> {
	
}
